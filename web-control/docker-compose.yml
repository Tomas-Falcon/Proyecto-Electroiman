services:
  web-control:
    build: .
    container_name: electroiman-web-control
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
